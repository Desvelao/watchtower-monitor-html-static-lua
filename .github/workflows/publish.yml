name: Publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

jobs:
  format-check:
    uses: ./.github/workflows/format-check.yml

  build-and-push:
    needs: format-check
    uses: ./.github/workflows/docker-publish.yml
